add_namespace = ukraine

country_event = {
	id = ukraine.1
	title = ukraine.1.t
	desc = ukraine.1.d
	picture = GFX_event_UKR_invasion
	
	is_triggered_only = no
	fire_only_once = yes
	
	trigger = {
		tag = UKR
		AND = {
			date > 1936.1.2
		}
	}
	
	mean_time_to_happen = {days = 1}
	
	option = { 
		name = ukraine.1.a
		ai_chance = { factor = 100 }
		declare_war_on = {
			target = DNR
			type = annex_everything
		}
		declare_war_on = {
			target = LNR
			type = annex_everything
		}
		DNR = {
			add_ideas = {
				NVR_boost
			}
			create_faction = "Новороссия"
			add_to_faction = LNR
		}
		LNR = {
			add_ideas = {
				NVR_boost
			}
		}
	}
}