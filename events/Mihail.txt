add_namespace = mihail

country_event = {
	id = mihail.1
	title = mihail.1.t
	desc = mihail.1.d
	picture = GFX_event_MIH_offensive_sov
	
	is_triggered_only = no
	fire_only_once = yes
	
	trigger = {
		tag = MIH
		AND = {
			date > 1936.1.1
		}
	}
	
	mean_time_to_happen = {days = 1}
	
	option = { 
		name = mihail.1.a
		ai_chance = { factor = 100 }
		declare_war_on = {
			target = SOV
			type = annex_everything
		}
		add_ideas = {
			MIH_zov
		}
	}
}